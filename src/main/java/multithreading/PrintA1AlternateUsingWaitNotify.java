package multithreading;

/*
wait() and notify()
wait()
purpose: Causes the current Thread to pause its execution and release the
associated monitor(lock) until another thread invokes notify() or notifyAll()
for the same object

why:
 synchronisation between threads
 pausing a thread's execution until a certain condition is met

 notify() & notifyAll()
 Purpose: Wakes up a single thread(notify()) or all threads(notifyAll()) that are waiting on the objects monitor due to a wait() call

 */
public class PrintA1AlternateUsingWaitNotify {

    public static void main(String[] args) {



    }

}
